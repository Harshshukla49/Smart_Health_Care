import { app, auth, firebaseApp, firebaseAuth, firebaseConfig, missingFirebaseConfigKeys } from '../services/firebase';

export { app, auth, firebaseApp, firebaseAuth, firebaseConfig, missingFirebaseConfigKeys };
export default app;
